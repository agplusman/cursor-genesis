# 回流待审批区
#
# 目录结构：
# pending/
# └── {project-hash}/
#     └── {contributor}/
#         └── {commit-id}/
#             └── content.md
#
# 回流内容限制：
# - 必须是实际应用中使用过的
# - PR 提交时不需要知道最终目录
# - 是否有价值、放哪里由项目自治决定
