# cursor-genesis 节点元信息
# 遵循 leaf-node-framework 规范

id: cursor-genesis
name: Cursor 协作模板
version: 1.0.0
domain: 工具-Cursor
description: |
  Cursor AI 协作研发的组件库和知识库。
  包含 Rules、Capabilities、Patterns、Code Templates 等可复用资产。

# 框架配置（预留，待 knowledge-graph 建立后启用）
framework:
  source: knowledge-graph/leaf-node-framework
  version: v1.0
  pipeline: standard

# 节点职责
responsibilities:
  - 生产 Cursor 相关的组件和知识
  - 接收使用方的回流改进
  - 暴露标准化索引供上层检索

# 不负责的事项
out_of_scope:
  - 跨领域知识关联（上层职责）
  - 治理框架设计（leaf-node-framework 职责）
  - 知识体系定位（上层 domains.yaml 定义）

# 生命周期
lifecycle:
  created: 2026-02-25
  status: active

# 版本记录
changelog:
  - version: 1.0.0
    date: 2026-02-25
    changes:
      - 初始结构创建
      - 从 personal_knowlegy/v1 迁移
