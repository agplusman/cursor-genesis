---
description: 验收模态工作指引——标书对标、菜单规划、演示脚本
globs: docs/acceptance/**
---

# 验收模态工作指引

> 服务模态：验收模态
> 核心素材：`docs/acceptance/acceptance-menu-and-demo-plan.md`

## Scope：管了什么

| 文件 | 内容 |
|:---|:---|
| `acceptance-menu-and-demo-plan.md` | 四大系统的完整验收菜单树 + 演示流程脚本（标书逐条提取） |
| `new-pages-classification-checklist.md` | 新页面分类检查清单（管理页面 vs 业务流程的区分） |

## Workflow：怎么用

### 1. 确认验收菜单覆盖率

**首选索引**：`acceptance-menu-and-demo-plan.md` **§5**（标书项 → 实现证据 → 状态），已与 `moduleRegistry.ts`、`menu.ts`（含 `DASHBOARD_ENTRIES`）对齐。

再对照同文档 §一～四 的完整菜单树与运行时注册表：
- 已有 → 标记 ✅
- 缺失 → 标记差异，评估实现优先级

### 2. 编写/完善演示脚本

每个演示流程（F1, F2, ...）需包含：
- 涉及页面（按操作顺序）
- 演示要点（要看到什么效果）
- 前置数据（需要提前准备的 mock 数据）

### 3. 新页面归类

新页面出现时，按 `new-pages-classification-checklist.md` 判断：
- 是**管理页面**（需独立菜单项）还是**业务流程**（需演示脚本但不一定是独立菜单）
- 标书原文的描述是"管理"还是"操作/流程"

## Extension：怎么扩展

- 新增验收菜单项：**先在** `moduleRegistry.ts`（及大屏时 `menu.ts` 的 `DASHBOARD_ENTRIES`）注册，再更新 `acceptance-menu-and-demo-plan.md` §5 与 §一～四；可选同步 `frontend/src/config/acceptanceMenu.ts`（文档用辅助，非运行时数据源）
- 新增演示流程：在对应子系统的"演示流程大纲"表格追加行
- 标书中找到之前遗漏的功能：先归类（管理页面/业务流程），再追加

## Cross-ref：改了这里还要看哪里

- 验收菜单变更 → 检查 `moduleRegistry.ts` 是否需要同步注册
- 演示流程变更 → 检查相关模块的 `model.yaml` 是否支持演示所需的操作
- 新增页面 → 检查对应子域的 ontology 是否覆盖该功能的业务概念

## Agent 行为

- 验收模态下**严格参照标书**：`docs/architecture/安全管理中心最终版-to雅楠.txt` 和 `安全大脑需求规格说明书-身份管理.md`
- 不自由发挥——标书没写的功能不加到验收菜单
- 已有的 `acceptance-menu-and-demo-plan.md` 是从标书逐条提取的，以此为权威参考
- 如果用户问"验收还差什么"，对比当前实现与验收菜单清单，列出差异
