---
description: 三模态产品架构——同一系统对不同受众的投影
alwaysApply: true
---

# 产品模态感知

> 设计文档：`docs/domain/product-mode-architecture.md`
> 推导记录：`knowledge-graph/meta/derivation/three-mode-product-architecture-2026-03-13.md`
> 实现状态：**已落地**（Phase 0-4 全部完成，2026-03-14）

## 三种模态

| 模态         | 受众                | 菜单结构                     | 字段范围                        | viewMode toolbar/rowActions | 专属能力                          | 隐藏什么                     |
| :----------- | :------------------ | :--------------------------- | :------------------------------ | :-------------------------- | :-------------------------------- | :--------------------------- |
| **开发模态** | 开发者 / 系统管理员 / 现场项目经理 | 后端包 + 技术 devSubGroup    | 全量（列和字段不裁剪）         | **生效**（同用户模态） | Model Editor（可视化编辑 model.yaml） | hiddenColumns 不裁剪       |
| **验收模态** | 评审方              | 标书四级菜单                 | 标书子集（`modes.acceptance`）  | 生效 | AcceptanceBubble（合同项映射悬浮球）  | 技术模块、Model Editor       |
| **用户模态** | 业务操作人员        | 同开发模态                   | 开发 - PM 隐藏（`modes.user`） | 生效 | 运行时字段可见性调整              | Model Editor、技术配置模块   |

## 实现架构

```text
modeStore (Pinia, localStorage 持久化)
  ↓ currentMode: 'dev' | 'acceptance' | 'user'
  ├── menu.ts: buildMenuByMode() → 按 devGroup 或 acceptanceGroup 渲染菜单
  ├── ListPageEngine: viewModeOverrides toolbar/rowActions → 全模态生效（dev 不跳过）
  ├── ListPageEngine: isFieldHiddenByMode() → 按 modes 裁剪列/查询/表单字段（dev 不裁剪）
  ├── ListPageEngine: hiddenColumns → 仅 non-dev 模态裁剪
  ├── ModelEditor: v-if="modeStore.isDevMode" → 仅开发模态可用
  └── AcceptanceBubble: v-if="isAcceptanceMode" → 仅验收模态显示
```

> **设计原则**（2026-03-14）：Dev 模态 = 全量版用户模态。viewMode 的 toolbar/rowActions 在所有模态下生效，dev 只多出全量字段可见 + Model Editor + 技术模块。推导记录：`knowledge-graph/meta/derivation/dev-mode-as-full-user-mode-2026-03-14.md`

### 关键数据流

| 组件 | 数据源 | 行为 |
|:---|:---|:---|
| **菜单** | `moduleRegistry.ts` 的 `devGroup`/`acceptanceGroup` | dev/user → 按 devGroup 分组；acceptance → 按 acceptanceGroup 分组 |
| **字段过滤** | schema.ts 每个 column/field 的 `modes` 字段 | `modes.user.hidden` 或 `modes.acceptance.hidden` → 该字段在对应模态不渲染 |
| **模块可见性** | `moduleRegistry.ts` 的 `acceptanceHidden`/`userHidden`/`acceptanceOnly` | 控制模块级别在菜单中的显示/隐藏 |
| **合同项映射** | `acceptanceMapping.ts` 的 objectCode → 标书条款 | AcceptanceBubble 按当前路由匹配显示 |
| **Model Editor** | Vite dev API (`/api/dev/model/:module`) → model.yaml | GET 读取 → 编辑 → PUT 写回 → gen-schema → HMR 热更新 |

## 模态识别信号

| 信号关键词                                 | 模态                 |
| :----------------------------------------- | :------------------- |
| "验收""演示""标书""合规""检查"             | 验收模态             |
| "配置""元数据""字段管理""系统维护""开发"   | 开发模态             |
| "用户""简化""产品经理说""业务操作""用起来" | 用户模态             |
| 无明确信号                                 | **开发模态（默认）** |

## 可配置性边界（双条件模型）

决定"这个功能应该让用户在页面上操作吗"时，两个条件共同判断：

**条件 1（安全门）**：操作可逆吗？

| 操作层级 | 代表                       | 可逆性   | 通过安全门？ |
| :------- | :------------------------- | :------- | :----------- |
| 元数据层 | 加字段、改可见性、调排序   | 完全可逆 | 是           |
| 模板层   | 切换列表布局、表单分组     | 可逆     | 是           |
| 逻辑层   | 校验规则、联动动作、工作流 | 部分可逆 | 否→仅开发模态 |
| 代码层   | 自定义组件、特殊交互       | 不可逆   | 否→仅开发者   |

**条件 2（驱动力）**：用户觉得方便吗？

通过安全门的功能，是否暴露取决于用户需要——字段太多想隐藏、创建太复杂想合并、布局不好想切换，这些诉求才是真正的驱动力。可逆但用户不需要的功能不暴露（减少噪音）。

**决策流**：可逆 + 想要 = 给 | 可逆 + 不需要 = 不暴露 | 不可逆 = 不给

## 两轨开发模式

| 轨道 | 操作方式 | 适用场景 | 代表功能 |
| :--- | :------- | :------- | :------- |
| **低代码轨** | Model Editor 编辑 model.yaml → gen-schema → HMR 即时生效 | 字段/展示/查询/模态可见性调整 | Model Editor、modes 配置 |
| **代码轨** | 后端编写代码 + AI 辅助 | 复杂/特殊化的业务逻辑 | 自定义组件、跨域组合查询 |

趋势：通用能力逐步从代码轨提炼到低代码轨。开发模态同时操作两轨；用户模态只操作低代码轨。

## Agent 行为

- 识别当前模态后，在回复开头标注：`[开发模态]` / `[验收模态]` / `[用户模态]`
- 验收模态下：一切参照 `docs/acceptance/` 下的标书映射，不自由发挥
- 用户模态下：只建议元数据层和模板层的调整，不涉及逻辑层和代码层
- 讨论"用户能不能操作 X"时：先判可逆性（安全门），再判用户是否需要（驱动力）
- 模态不明确时默认开发模态，但若用户提到产品经理或用户习惯，主动提示切换到用户模态
- 设计新功能时，主动评估"这个功能哪部分可以走低代码轨？"
- 新增字段时提醒：是否需要配置 `modes` 标注模态可见性？
