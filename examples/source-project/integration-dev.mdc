---
description: 外部系统集成规范——接口目录、数据格式、对接流程
globs: docs/integration/**
---

# 集成开发指南

> 服务模态：**开发模态**

## 【Scope】管了什么

| 目录 | 内容 |
|:---|:---|
| `interface-catalog.md` | 外部接口总目录 |
| `systems/` | 6 个外部系统的集成规范（防火墙、WAF、APT、零信任、日志审计、审计中心） |
| `data-formats/` | 3 种数据格式（安全事件、认证事件、Syslog 解析） |
| `deployment/` | 网络需求、Mock→生产切换指南 |

## 【Workflow】怎么用

### 对接新外部系统

1. 读 `interface-catalog.md` 确认是否已有规范
2. 读对应系统文档（`systems/<系统>.md`）了解接口协议
3. 读对应数据格式（`data-formats/<格式>.md`）了解数据结构
4. 在 `ExternalSystemService` 中实现对接逻辑
5. Mock 阶段用 `mock-server/` 模拟，遵循 `deployment/mock-to-prod-switch-guide.md`

## 【Extension】怎么扩展

- 新外部系统：在 `systems/` 下新建文档，更新 `interface-catalog.md`
- 新数据格式：在 `data-formats/` 下新建文档

## 【Cross-ref】改了这里还要看哪里

| 这里变了 | 同步检查 |
|:---|:---|
| 新增外部系统 | → Mock server 是否需要新增模拟接口 |
| 数据格式变更 | → 后端解析逻辑是否同步 |
