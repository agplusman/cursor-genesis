---
description: 实体边界与组合原则。在设计新功能、差距分析、模块规划时自动应用。
globs:
alwaysApply: true
---
# 实体边界与组合原则

> 设计文档：`docs/domain/meta-engine-architecture.md` §七-§九
> 推导记录：`knowledge-graph/meta/derivation/entity-boundary-and-composition-2026-03-24.md`

## 核心规则

**一个 model.yaml 只包含实体自己拥有的字段。别人的东西通过引用组合，不往主体表上堆。**

判断方法：

1. **这个数据的生命周期由谁管？** → 由谁管就放在谁的 model.yaml
2. **这个数据被其他实体引用吗？** → 被引用就必须独立模块

## 差距分析视角矫正

当执行差距分析（gap analysis）或评估覆盖率时，**禁止**以下思路：

| 错误思路 | 正确思路 |
|:---|:---|
| "personnel_info 缺少可信设备字段" | "可信设备绑定是独立模块，需要 scaffold" |
| "Identity 子域覆盖度 40%" | "Identity 4 个主体表完整，缺 6 个卫星模块" |
| "asset_info 缺少告警计数字段" | "告警数据来自 posture 域，通过跨域查询组合" |

**缺口的正确分类**：

- **缺主体字段** → 主体 model.yaml 补字段（少见）
- **缺卫星模块** → 新建独立 model.yaml + scaffold（常见）
- **缺跨域组合** → 在视图/详情页中引用其他域的 API（常见）

## 引用数据谱系

选项数据的载体由**值的性质**决定，不是由"几个模块用"决定：

| 判断 | 载体 | 示例 |
|:---|:---|:---|
| 代码对具体值做 if/switch 分支 | `type: enum`（必须） | lifecycleStatus、mainType、审批动作 |
| 值有树形/层级/自带属性 | `type: ref`（独立管理表） | asset_type、organization、vulnerability_type |
| 平铺 label/value，只有开发者改 | `type: enum`（当前最低成本） | personnelType、威胁级别 |
| 平铺 label/value，管理员需要按站点定制 | 轻量 ref 表或 dict（dict 管线待建） | 未来：站点特定的分级标准 |

**核心判断：系统需不需要理解每个具体值的含义？需要 → enum（行为型），不需要 → 看结构和治理。**

## 能力组合模式

领域功能不一定是独立模块，也可以是通用能力的插件：

| 通用能力 | 插件方式 | 已有实例 |
|:---|:---|:---|
| 任务调度 | model.yaml `schedule` 段 + JobHandler | asset-discover-task |
| 条件规则 | 注册 sceneCode + SceneProvider | asset_ignore、asset_classify |
| 审批流转 | 审批模板 + 回调 | 资产上架审批 |

**决策**：有独立 CRUD 生命周期 → 独立模块；只是行为/动作 → 通用能力插件。两者可组合（独立配置 + 通用调度）。

## Agent 行为

- 评估"缺了什么"时，先判断缺的是主体字段、卫星模块、还是跨域组合
- 设计新字段时，先问"这个数据的生命周期由谁管"
- 设计新选项时，走引用数据谱系决策流
- 设计新功能时，评估是独立模块还是通用能力插件
