---
description: 技术调研规范——已有调研成果索引、新调研流程
globs: docs/research/**
---

# 技术调研指南

> 服务模态：**开发模态**

## 【Scope】管了什么

| 调研主题 | 状态 | 结论文件 |
|:---|:---|:---|
| SOC 系统架构 | 已完成 | `soc-system-architecture/report.md` |
| 轻量级规则引擎 | 已完成 | `lightweight-rule-engine/COMPARISON-AND-RECOMMENDATION.md`（选型：SpEL） |
| Java 任务调度 | 已完成 | `java-task-scheduler/COMPARISON-AND-RECOMMENDATION.md`（选型：JobRunr） |
| 本体-元数据集成 | 已完成 | `extracted/ontology-metadata-integration/`（已回写本体） |
| 安全中心设计讨论 | 已完成 | `extracted/security-center-design-discussion/` |

素材索引：`source-index.md`

## 【Workflow】怎么用

### 场景 1：查阅已有调研结论

直接读对应主题的 `COMPARISON-AND-RECOMMENDATION.md` 或 `report.md`。

### 场景 2：新技术调研

使用 deep-research 流程（已安装 pack）：

1. 在 `docs/research/` 下创建主题目录
2. 按 `raw/` → `fetched/` → `notes/` → `COMPARISON-AND-RECOMMENDATION.md` 四层结构组织
3. 结论文件必须包含：候选方案对比表 + 推荐方案 + 理由

### 场景 3：调研结论落地

1. 读结论文件确认选型
2. 若涉及新依赖：更新 `pom.xml` / `package.json`
3. 若涉及架构决策：记录到 `docs/domain/_technical/`

## 【Extension】怎么扩展

新调研主题目录结构：

```
docs/research/<主题名>/
├── raw/           # 原始搜索结果
├── fetched/       # 拉取的参考页面
├── notes/         # 研究笔记（每个任务一个文件）
└── COMPARISON-AND-RECOMMENDATION.md  # 结论
```

## 【Cross-ref】改了这里还要看哪里

| 这里变了 | 同步检查 |
|:---|:---|
| 新调研结论 | → 是否影响 `_technical/modules.yaml` 的技术选型 |
| 设计讨论落位建议 | → 按 `ontology-maintenance-guide.md` 同步本体 |
