---
description: 运维测试——测试清单、运行命令、基础设施说明。当用户提到"跑测试""运行测试""test""单测""集成测试""e2e"时激活。
globs: backend/src/test/**, frontend/**/__tests__/**, frontend/e2e/**
---
# 运维测试指南

## 前置条件

| 依赖 | 用途 | 检查命令 |
|:---|:---|:---|
| JDK 17+ | 后端编译和测试 | `java -version` |
| Maven | 后端构建 | `mvn -v` |
| Docker Desktop | 集成测试 Testcontainers | `docker info` |
| Node.js 18+ | 前端测试 | `node -v` |

集成测试依赖 Testcontainers 启动 MySQL 8 容器，Docker Desktop 必须运行且开放 TCP 2375。

## 测试清单

### 后端 — 基础设施

| 文件 | 类型 | 作用 |
|:---|:---|:---|
| `common/BaseIntegrationTest.java` | 抽象基类 | Testcontainers MySQL 8 单例容器 + schema.sql 初始化 |
| `docs/architecture/known-simplifications.md`（附录 B） | 架构说明 | compose / Testcontainers / 部署 的 DB 初始化差异：`schema.sql` 同源、warm-flow 与种子仅 compose、volume 与 ECS 漂移等 |
| `common/AbstractCrudControllerTest.java` | 模板 | 标准 CRUD Controller 集成测试（7 CRUD + 3 启用/禁用 + 1 导出 = 11 个通用用例），子类只需提供 API 路径 + JSON fixture，启用/禁用需覆写 `supportsEnableDisable()` |
| `common/AbstractCrudServiceTest.java` | 模板 | 标准 CRUD Service 单元测试（9 CRUD + 7 启用/禁用 + 8 生命周期 = 24 个通用用例），子类只需提供 fixture，启用/禁用需覆写 `supportsEnableDisable()` + `entityWithEnableState()` |

### 后端 — 资产模块（asset）

| 文件 | 类型 | 用例数 | 覆盖范围 |
|:---|:---|:---|:---|
| `asset/service/AssetServiceTest.java` | 单元测试（Mockito） | 32 | Create(3), Promote(7), Archive(7), Ignore(6), ApprovalStatus(2), IdentityRegistration(6), Helper(2) |
| `asset/controller/AssetControllerTest.java` | 集成测试（Testcontainers） | 24 | List+过滤(5), GetById(2), Create(1), Update(1), Delete+批量(2), Promote(5), Archive(3), Ignore(5) |

### 后端 — 资产子模块 CRUD（gen-test.js 生成）

继承 `AbstractCrudControllerTest`(7 cases) + `AbstractCrudServiceTest`(9 cases)：

| 模块 | Controller 测试 | Service 测试 |
|:---|:---|:---|
| asset-type | `AssetTypeCrudControllerTest` | `AssetTypeCrudServiceTest` |
| asset-grade | `AssetGradeCrudControllerTest` | `AssetGradeCrudServiceTest` |
| asset-domain | `AssetDomainCrudControllerTest` | `AssetDomainCrudServiceTest` |
| asset-meta-field | `AssetMetaFieldCrudControllerTest` | `AssetMetaFieldCrudServiceTest` |
| discover-task | `AssetDiscoverTaskCrudControllerTest` | `AssetDiscoverTaskCrudServiceTest` |
| discover-result | `AssetDiscoverResultCrudControllerTest` | `AssetDiscoverResultCrudServiceTest` |

小计：6 模块 × ~16 cases = **~96 cases**

### 后端 — 行为扩展通用服务

| 文件 | 类型 | 用例数 | 覆盖范围 |
|:---|:---|:---|:---|
| `common/behavior/IgnoreRuleServiceTest.java` | 单元测试（Mockito） | 9 | ignore 分发(2), unignore 分发(2), 未知场景(2), null targetId(2), 不匹配 path+scene(1) |

### 后端 — 任务中心（task）

| 文件 | 类型 | 用例数 | 覆盖范围 |
|:---|:---|:---|:---|
| `task/controller/TaskControllerTriggerVerificationTest.java` | 集成测试（MockJobRegistrar） | 2 | 触发 mock job 成功、触发不存在 job 返回错误 |

### 工具层 — gen-schema 行为生成器

| 文件 | 类型 | 用例数 | 覆盖范围 |
|:---|:---|:---|:---|
| `tools/generators/__tests__/behaviors.test.js` | Node.js 单元测试 | 22 | enableDisable(3), sourceTracking(3), audit(2), dedup(3), ignoreRule(2), export(4), lifecycle(4), disposition(2), 组合(1) |

> 运行命令：`cd tools && node --test generators/__tests__/behaviors.test.js`

### 前端 — 单元测试（Vitest）

#### 手写测试（核心引擎 + 资产定制）

| 文件 | 用例数 | 覆盖范围 |
|:---|:---|:---|
| `components/__tests__/ListPageEngine.test.ts` | 25 | Schema 加载渲染(3), 模态字段过滤(5), ViewMode 覆盖(2), 开发模态专属(3), 错误处理(1), behaviorConfig-enableDisable(2), behaviorConfig-sourceTracking(1), behaviorConfig-schema 结构(5), behaviorConfig-toolbar(1) |
| `views/asset/__tests__/adapter.test.ts` | 12 | adaptAssetList 转换/边界(4), adaptAssetDetail 转换/空值/回退(8) |
| `views/asset/__tests__/schema.test.ts` | 15 | 结构正确性(3), 身份(2), ViewMode(6), 工具栏/行操作(5), 生命周期(1), 布局(1), 模态字段(4) |
| `utils/__tests__/fieldVisibility.test.ts` | 13 | isFieldHiddenByMode: dev(1), modes.hidden(4), applicableTo(4), 组合(3), TYPE_VIEW_MODES(2) |

#### 验收专项测试（手写）

| 文件 | 用例数 | 覆盖范围 |
|:---|:---|:---|
| `views/asset/__tests__/acceptance-fields.test.ts` | 12 | modes.acceptance.hidden 配置(2), hiddenColumns 完整(2), applicableTo 一致性(3), required 字段(2), user mode hiddenColumns(1), 交叉验证(2) |
| `config/__tests__/acceptance-mapping.test.ts` | 7 | 9 页面 mapping 完整性(1), contractRef 非空(1), section 归属(1), menuPath↔moduleRegistry 一致(1), getAcceptanceRef(2), demoFlows 非空(1) |

#### 批量生成测试（gen-test.js — schema 结构验证）

由 `tools/gen-test.js` 从 `model.yaml` + `modules.yaml` 自动生成，每个模块 ~7 个用例：通用结构校验、objectCode/objectName、必填字段、查询字段、toolbar/rowActions 中文标签。

| 域 | 模块数 | 测试文件数 | 用例数 |
|:---|:---|:---|:---|
| asset | 9 | 8（asset 手写 + asset-grade 生成） | ~56 |
| identity | 4 | 4 | ~28 |
| policy | 12 | 12 | ~84 |
| affairs | 10 | 10 | ~70 |
| posture | 3 | 3 | ~21 |
| knowledge | 6 | 6 | ~42 |
| system | 1 | 1 | ~7 |
| **小计** | **45** | **44** | **~324** |

> 生成命令：`cd tools && node gen-test.js --frontend`
> 每个测试位于 `views/<域>/<模块>/__tests__/schema.test.ts`

### 前端 — E2E 测试（Playwright）

#### 通用

| 文件 | 用例数 | 覆盖范围 |
|:---|:---|:---|
| `e2e/asset.spec.ts` | 12 | IP 视图 CRUD 全链路(9), 视图切换(1), 模态切换(1), 表单校验(1) |
| `e2e/acceptance-walkthrough.spec.ts` | ~20 | 验收模态页面加载烟测(13), 验收专属UI(3), F1流程走读(1), F11流程走读(1), 跨子系统导航(1) |
| `e2e/engine-behaviors.spec.ts` | ~14 | 页面加载(2), P1 启用/禁用切换+编辑拦截(2), P2 来源追溯表单+默认值(2), P4 导出(1)+去重(1)+忽略(1), 综合 toolbar/rowActions 中文+搜索联动(3) |

#### 验收资产全量（`e2e/acceptance-asset/`）

共享 fixture：`e2e/fixtures/acceptance-asset.ts`（445 行，4 类资产池 + 临时/归档 + 分类树/分级树/元数据/画像/审批 mock）

| 文件 | 用例数 | 覆盖范围 |
|:---|:---|:---|
| `acceptance-asset/digital-asset-crud.spec.ts` | ~40 | IP/服务/应用/数据 4 视图 × CRUD + 搜索 + 分页 + 字段可见性 + 特殊操作（注册设备身份/应用身份） |
| `acceptance-asset/temporary-asset.spec.ts` | 10 | 注册/上架(promote)/忽略/编辑/查看/搜索/批量删除/hiddenColumns |
| `acceptance-asset/asset-profile.spec.ts` | 4 | 画像页加载/Tab 结构/基本信息/AcceptanceBubble |
| `acceptance-asset/asset-config.spec.ts` | 14 | 分类树 CRUD(5) + 分级树 CRUD(4) + 元数据 CRUD(5) |
| `acceptance-asset/acceptance-ui.spec.ts` | ~31 | 9 页面字段隐藏 + AcceptanceBubble(9) + ModelEditor 隐藏(9) + hiddenColumns(5) + 菜单结构(3) |
| `acceptance-asset/demo-flows.spec.ts` | 9 | F1-F9 全部演示流程跨页面走读（mock 状态串联） |

### 总计

| 层级 | 用例数 |
|:---|:---|
| 后端单元测试 | 32 + 9（IgnoreRuleService）= 41 |
| 后端集成测试（手写） | 26 |
| 后端集成测试（资产子模块 gen-test） | ~96 |
| 工具层（behaviors.js） | 22 |
| 前端手写单元测试 | 73 + 11（behaviorConfig）= 84 |
| 前端 gen-test 批量测试 | ~324（重新生成后 behaviorConfig 段会新增 ~2-6 cases/模块） |
| 前端 E2E（通用） | ~32 + 14（engine-behaviors）= ~46 |
| 前端 E2E（验收资产全量） | ~108 |
| **合计** | **~801+** |

> 行为扩展带来的增量：AbstractCrudServiceTest 新增 7 enable/disable 用例（模块覆写后自动启用）；AbstractCrudControllerTest 新增 4 用例（同理）；gen-test.js 重新生成后每个有 enableDisable 的模块额外获得 enable/disable 测试。

## 运行命令

### 后端

```powershell
# 全部后端测试
cd backend && mvn test

# 资产 Service 单元测试（无需 Docker，~5s）
cd backend && mvn test "-Dtest=com.secmgr.asset.service.AssetServiceTest"

# 资产 Controller 集成测试（需要 Docker，~70s）
cd backend && mvn test "-Dtest=com.secmgr.asset.controller.AssetControllerTest"

# 资产子模块 CRUD 测试（需要 Docker）
cd backend && mvn test "-Dtest=com.secmgr.asset.controller.*CrudControllerTest"
cd backend && mvn test "-Dtest=com.secmgr.asset.service.*CrudServiceTest"

# 任务中心集成测试
cd backend && mvn test "-Dtest=com.secmgr.task.controller.TaskControllerTriggerVerificationTest"

# 按包跑（资产全部）
cd backend && mvn test "-Dtest=com.secmgr.asset.**"
```

### 前端

```powershell
# 全部 Vitest 单元测试
cd frontend && npx vitest run

# 验收专项单元测试
cd frontend && npx vitest run src/views/asset/__tests__/acceptance-fields.test.ts src/config/__tests__/acceptance-mapping.test.ts

# 单个文件
cd frontend && npx vitest run src/components/__tests__/ListPageEngine.test.ts

# E2E（需要前端 dev server 运行）
cd frontend && npx playwright test

# E2E 验收资产全量
cd frontend && npx playwright test e2e/acceptance-asset/

# E2E 单文件
cd frontend && npx playwright test e2e/asset.spec.ts

# E2E 带 UI
cd frontend && npx playwright test --ui
```

### PowerShell 注意事项

Maven 的 `-D` 参数在 PowerShell 中需要用双引号包裹，否则会被解析为 cmdlet 参数：

- 正确：`mvn test "-Dtest=com.secmgr.asset.**"`
- 错误：`mvn test -Dtest=com.secmgr.asset.**`

## 扩展新模块测试

### 后端 — 继承抽象基类快速生成

对于走 AbstractCrudService 的标准模块，只需继承模板即可获得全套测试：

**Service 单元测试**：继承 `AbstractCrudServiceTest<E>`，提供 fixture（实体、Mock Mapper、Service 实例）。

**Controller 集成测试**：继承 `AbstractCrudControllerTest`，提供 API 路径 + JSON fixture，加 `@Sql("classpath:db/data.sql")`。

对于 AssetService 这类不继承 AbstractCrudService 的独立模块，需手写完整测试。

### 前端 — Schema 结构测试

推荐使用 gen-test.js 批量生成：

```powershell
cd tools && node gen-test.js --frontend                    # 全部模块
cd tools && node gen-test.js --frontend --module alert     # 单个模块
```

每个生成的测试至少验证：

- tableColumns / searchFields / formFields 结构正确（通用 `validateSchemaStructure`）
- objectCode / objectName 匹配
- 必填字段标记 required
- 查询字段存在
- toolbar / rowActions 标签全部为中文
- viewModeOverrides（如有）完整
- **behaviorConfig 是对象**（所有模块）
- **enableDisable / sourceTracking / audit / dedup / ignoreRule / export 配置正确**（当 model.yaml 启用对应行为时）
- **无行为时 behaviorConfig 为空对象**

## 工具

| 工具 | 路径 | 用途 |
|:---|:---|:---|
| gen-test.js | `tools/gen-test.js` | 从 model.yaml + modules.yaml 批量生成前端 schema 测试 + 后端 Controller/Service 测试 + E2E 骨架 |
| gen-schema.js | `tools/gen-schema.js` | 从 model.yaml 生成 schema.ts + api/index.ts |

### E2E 骨架生成（gen-test.js --e2e）

```powershell
cd tools && node gen-test.js <module> --e2e          # 生成 fixture + spec
cd tools && node gen-test.js <module> --e2e --dry    # 预览不写文件
```

输出：
- `frontend/e2e/fixtures/<objectCode>.ts` — 数据工厂 + 有状态 Mock + Helpers
- `frontend/e2e/<objectCode>.spec.ts` — 基础 CRUD 测试骨架（6-8 个用例）

生成后需手写完善：领域特定操作、模态切换测试、跨页面流程。
方法论详见 `knowledge-graph/meta/derivation/e2e-test-methodology-from-practice-2026-03-15.md`。

## 已知覆盖缺口

| 模块 | 缺口 | 优先级 |
|:---|:---|:---|
| identity（4 模块） | 无后端测试 | P2 |
| policy（12 模块） | 无后端测试 | P3 |
| affairs（10 模块） | 无后端测试 | P3 |
| posture（3 模块） | 无后端测试 | P3 |
| knowledge（6 模块） | 无后端测试 | P3 |
| integration | 适配器/心跳无测试 | P2 |
| ~~验收走读 E2E~~ | ~~烟测+流程走读已有，需扩展有状态交互~~ | ~~P3~~ **已完成**：`e2e/acceptance-asset/` 6 个 spec 覆盖 |
| 验收 E2E 其他域 | identity/policy/affairs/posture/knowledge 验收全量 E2E | P3 |
| ~~行为扩展（enableDisable/sourceTracking/audit/dedup/ignoreRule/export）~~ | ~~全栈无测试~~ | ~~P1~~ **已完成**：behaviors.test.js + AbstractCrudServiceTest 扩展 + AbstractCrudControllerTest 扩展 + IgnoreRuleServiceTest + ListPageEngine behaviorConfig 测试 + engine-behaviors.spec.ts E2E |
| AuditAspect | AOP 切面集成测试（需 Spring context + 验证审计日志落库） | P3 |
| ExportTaskService | 异步导出任务流程集成测试 | P3 |
