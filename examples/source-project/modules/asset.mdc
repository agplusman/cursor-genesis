---
description: 资产管理模块开发规范（含元数据驱动、模态差异）
globs: backend/**/asset/**,frontend/**/asset/**,docs/domain/asset/**,docs/domain/asset-*/**
---

# 资产管理模块规范

## 领域模型

- **核心实体**：SecurityAsset（安全资产）
- **卫星/扩展**：AssetType（类型）、AssetDomain（域）、AssetCategory（分类）、AssetDiscovery（发现）、AssetArchive（归档）、AssetIgnore（忽略）、AssetMetaField（元数据）
- **本体**：`docs/domain/asset/ontology-extracted.md`（核心）+ 各子模块 ontology
- **依赖方向**：被 policy/posture/affairs 引用，不依赖其他域

## 三层数据架构

| 层 | 表 | 写入者 | 字段类别 |
|:---|:---|:---|:---|
| **主表** | `asset_info` (~30 cols) | 人工管理操作、生命周期审批 | 管控决策、归属路由、安全标记 |
| **快照表** | `asset_snapshot` (~15 cols) | 扫描/同步任务、人工补充技术信息 | 采集事实（IP/MAC/OS/描述/位置） |
| **EAV** | `asset_meta_field` + `_data` (28 pre-seeded) | 管理员配置、用户录入 | 因人而异/可能变化的辅助属性 |

- **读取**：`v_asset_list` 视图（`asset_info LEFT JOIN asset_snapshot`），对 API 消费者透明
- **R4 铁规则**：扫描结果只更新 `asset_snapshot`，不动 `asset_info`
- **去重**：发现管线的 `dedupByUniqueKey` 查 `asset_snapshot`（ip_address/mac_address 在快照表）
- **原则**：不会变的、肯定有的 → 物理列；可能变的、因人而异的 → EAV

## 元数据驱动

- **Single Source of Truth**：`docs/domain/asset/model.yaml`（资产主体）+ 各子模块 `model.yaml`
- **Schema 生成**：修改 model.yaml 后运行 `cd tools && node gen-schema.js <模块名>`
- **禁止**：直接手改 `schema.ts`
- **EAV 扩展**：`asset-meta-field` 支持运行时新增字段，设计文档见 `docs/domain/asset/metadata-schema-design.md`

## 后端结构

- **包路径**：`com.secmgr.asset`
- **API 前缀**：`/api/v1/asset`（主体）、`/api/v1/asset-type`、`/api/v1/asset-domain` 等
- **核心服务**：AssetService（含 AssetSnapshotMapper 注入）、AssetMetaFieldService、AssetPostureService、AssetDiscoverTaskService/ResultService、AssetDomainService、AssetCategoryService
- **核心实体**：AssetInfo（主表）、AssetSnapshot（快照表）、AssetMetaField/AssetMetaFieldData（EAV）

## 前端结构

- **资产主页面**：`views/asset/index.vue`（唯一有自定义 Vue 的模块）
- **其他子模块**：ListPageEngine 动态渲染
- **组件模式**：useTable + useTableColumnWidth + el-table
- **列宽**：动态优化，见 `docs/frontend/list-column-width-optimization.md`

## 双字段类型模型（mainType + assetTypeId）

| 字段 | 性质 | 必填 | 作用 |
|:---|:---|:---|:---|
| **mainType** | 行为型枚举 (IP/SERVICE/APPLICATION/DATA) | 是 | 决定字段可见性（applicableTo）、视图路由（viewModes）、发现/同步任务分类 |
| **assetTypeId** | 组织型分类（ref → asset_type 树） | 否 | 管理员维护的细分分类（服务器/网络设备/交换机...），纯组织用途 |

mainType 像 lifecycleStatus 一样是固定枚举；assetTypeId 指向 asset_type 表的动态分类树。
创建资产时先选 mainType，引擎根据 mainType 动态显隐 applicableTo 字段。

## 字段可见性（applicableTo + modes 二维模型）

资产模块有 30+ 字段，通过两种正交机制控制可见性：

- **applicableTo**：字段适用哪种主类（ip/service/application/data）。不写 = 全类型可见
- **modes**：字段面向哪种受众。不写 = 全模态可见。dev 模态跳过两者，全量透出

字段维度分组：

| 维度 | 代表字段 | 存储层 | applicableTo |
|:---|:---|:---|:---|
| 管控核心 | assetName, assetCode, mainType, lifecycleStatus, CIA | asset_info | 全部 |
| 安全标记 | isClassifiedAsset, isExternallyExposed, isActiveOutbound | asset_info | 全部 |
| 网络采集 | ipAddress, macAddress, osType, osVersion | asset_snapshot | [ip, service, data] |
| 应用采集 | accessUrl, belongPlatform, parentAppId | asset_snapshot | [application] |
| 归属 | deptId, ownerId, assetDomainId | asset_info | 全部 |
| 来源/审计 | sourceTaskId, createBy, updateBy | asset_info | 全部，但 modes.user.hidden |
| EAV 扩展 | deviceModel, techStack, serviceName... | asset_meta_field_data | 按 applicable_asset_types |

**表单动态显隐**：用户在表单中选择 mainType 后，ListPageEngine 将 mainType 映射为 viewMode（IP→ip），对 applicableTo 字段实时过滤。从分视图（如 /asset/ip）进入时 mainType 自动预填。

viewModes 段的 hiddenColumns 由 gen-schema 从 applicableTo + defaultFilters 自动推导，不手动维护。

## 模态差异

| 维度 | 开发模态 | 验收模态 | 用户模态 |
|:---|:---|:---|:---|
| 入口结构 | 资产主表（技术验证）+ 4 类分视图 | 标书四级菜单 | 4 类分视图 + 临时/归档 |
| 列表字段 | 全量（忽略 applicableTo 和 modes） | 标书描述的字段 | applicableTo 过滤 + modes.user 裁剪 |
| 查询条件 | 全部 query: true | 标书描述的查询条件 | applicableTo 过滤 + modes.user 裁剪 |
| 操作 | 全量 CRUD + 生命周期 + 批量 + 导出 | 演示脚本 F1-F6 的操作 | viewMode 级 toolbar/rowActions |
| 元数据管理 | 全量 EAV CRUD（含展示控制开关） | 不展示或只读 | "加字段→列表显示"简化版 |

## 调试：概念完整性排查

遇到"下拉框没数据""缺字段""数据不对"等问题时，**自顶向下排查**，不要从代码/mock 反向搜索：

1. **本体**（应该有什么）：读 `docs/domain/asset/ontology-extracted.md` §二 关键概念 + §四 对象属性，确认该概念是否存在
2. **model.yaml**（配了什么）：检查对应字段的 `type: ref` / `ref.api` / `fallbackOptions`，与本体比对是否一致
3. **modules.yaml**（后端有没有）：读 `docs/domain/_technical/modules.yaml`，确认被引用的模块是否 `implemented: true`
4. **数据**（有没有种子数据）：检查 `backend/scripts/init-mysql.py` 或数据库，确认引用表有数据

本体 vs 实现的 diff = bug 来源。

## 业务约束

- 资产名称、资产类型必填
- IP 地址需 IP 格式校验
- 运行状态枚举：1 在线、2 离线、0 停用
- 生命周期：临时→已上架→归档（生命周期转换需审批）
