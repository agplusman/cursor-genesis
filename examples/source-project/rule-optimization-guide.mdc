---
description: 规则优化记录规范——Agent 进入 rule-optimizations 空间时触发
globs: docs/learnings/rule-optimizations/**
---

# 规则优化记录规范

## 本空间的作用

`docs/learnings/rule-optimizations/` 是规则体系优化的专属记录空间。

每次对 `.cursor/rules/` 做**行为逻辑层面**的改动后，在此创建一份记录，包含改了什么、为什么改、改前改后对比。目的：让后续 Agent 和人都能回溯优化历程，不重复踩坑。

## 记录时机

以下任一操作完成后，必须在本目录下创建记录：

- 修改 `.cursor/rules/*.mdc` 的路由逻辑、行为约束或排查流程
- 新增或删除规则文件
- 调整 `workflow-router.mdc` 的路由表
- 发现 Agent 行为路径低效并做出规则修正

**不需要记录**的：纯格式调整、补注释、修 typo。

## 文件命名

`YYYY-MM-DD-<简短主题>.md`

## 记录结构

每份记录必须包含以下段落（可增不可减）：

### 1. 头部元信息

标题、日期、触发场景。

### 2. 问题

描述观察到的 Agent 行为问题或规则缺陷。

### 3. 问题原因

根因分析：哪条规则缺失、哪个路由没覆盖、什么假设不成立。用表格列出"缺失 → 影响"。

### 4. 理想路径（对照组）

同一问题的最优解决路径。与实际路径形成鲜明对比——这是优化的标尺。

### 5. 优化方法（核心）

**每条优化独立一个子段**，格式固定：

- **原始**：改动前的规则状态或内容（引用或摘录）
- **优化后**：改动后的内容
- **理由**：为什么这样改

这三要素缺一不可——"原始 vs 优化后"的对比是本空间最核心的价值。

### 6. 优化结果

用表格对比量化指标（步数、读取次数、搜索次数等）。

### 7. 验证方式

如何验证优化已生效（通常是开新 chat 用同样问题测试）。

## 已有记录

进入本空间时，先扫一眼已有文件，了解历史优化方向，避免重复或矛盾。
