---
description: 技术本体维护规范——表结构、模块注册、概念映射
globs: docs/domain/_technical/**
---

# 技术本体维护

> 服务模态：**开发模态**

## 【Scope】管了什么

| 文件 | 内容 | 何时更新 |
|:---|:---|:---|
| `tables.yaml` | 全部表结构定义（含 `implemented` 标记） | 新增/修改表时 |
| `modules.yaml` | 前后端模块注册表（包名、路由、实现状态） | 模块 scaffold 完成时 |
| `mapping.yaml` | 业务概念 ↔ 技术实体映射 | 新概念落地或映射变更时 |
| `writing-guide.md` | 技术本体编写规范 | 规范变更时 |
| `ontology-implementation-audit.md` | 实现审计报告 | 审计时 |

### current / target 区分

- `implemented: true`：可直接引用，作为代码生成基准
- `implemented: false`：仅做规划参考，引用时注明"目标架构，当前未实现"

## 【Workflow】怎么用

### 新模块注册

1. 在 `modules.yaml` 的 `backend_modules` 追加条目
2. 填写 name、package、api_prefix、tables、implemented
3. 前端模块同步在 `frontend_modules` 追加
4. 完成后 `implemented` 改为 `true`

### 新表登记

1. 在 `tables.yaml` 追加表结构
2. 确认字段与业务本体中的数据属性对应
3. 更新 `mapping.yaml` 中概念→表的映射

## 【Extension】怎么扩展

- 编写规范见 `writing-guide.md`（表、模块、映射怎么填）
- 审计方法见 `ontology-implementation-audit.md`

## 【Cross-ref】改了这里还要看哪里

| 这里变了 | 同步检查 |
|:---|:---|
| `tables.yaml` 新增表 | → 对应子域 `ontology-extracted.md` 是否有该概念 |
| `modules.yaml` 改 implemented | → 前端 moduleRegistry 是否同步 |
| `mapping.yaml` 变更 | → 业务本体和技术本体是否一致 |
