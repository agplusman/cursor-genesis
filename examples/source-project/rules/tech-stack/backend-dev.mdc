---
description: 后端开发规范——架构分层、编码约定、模块 scaffold 流程
globs: backend/**
---

# 后端开发规范

> 从原 `project-conventions.mdc` 后端部分拆出，增加四要素结构
> 服务模态：**开发模态**

## 【Scope】管了什么

```
backend/src/main/java/com/secmgr/
├── asset/          # 资产管理（已实现）
├── identity/       # 身份管理（已实现）
├── task/           # 任务中心 - JobRunr 薄封装（已实现）
├── rule/           # 规则中心 - SpEL 条件匹配（已实现）
├── export/         # 导出服务（已实现）
├── integration/    # 外部系统集成（已实现）
├── policy/         # 策略控制（待实现，12 模块）
├── affairs/        # 事务处理（待实现，10 模块）
├── posture/        # 态势分析（待实现，3 模块）
├── knowledge/      # 安全知识（待实现，6 模块）
└── common/         # AbstractCrudService 等通用基类
```

## 【Workflow】怎么用

### 新模块 scaffold 标准流程

1. 读该模块的 `docs/domain/<子域>/<模块>/model.yaml`
2. 按 `_technical/modules.yaml` 确认包名、API 前缀、表名
3. 创建四层目录结构（见下方架构分层）
4. 继承 `AbstractCrudService`，只写差异化逻辑
5. API 路径使用 `/api/v1/` 前缀
6. 完成后更新 `_technical/modules.yaml` 中 `implemented: true`

### 架构分层

| 层 | 职责 | 包名后缀 |
|:---|:---|:---|
| Interface | Controller、DTO、VO | `.interfaces` |
| Application | 应用服务、编排 | `.application` |
| Domain | 领域模型、领域服务 | `.domain` |
| Infrastructure | MyBatis Mapper、外部服务 | `.infrastructure` |

### 编码约定

- 实体注释引用业务本体中文概念（`docs/domain/*/ontology-extracted.md`）
- 数据库字段与 `_technical/tables.yaml` 对应
- 异常统一用业务异常类包装
- RESTful 核心接口：`POST /list`、`GET /{id}`、`POST`、`PUT /{id}`、`DELETE /{id}`、`POST /batch-delete`

### 生成代码与自定义代码隔离（强制）

`gen-backend.js` 生成的文件**随时会被重新生成覆盖**，禁止在其中添加自定义逻辑。

| 文件 | 类型 | 可否手动修改 |
|:---|:---|:---|
| `*Entity.java` | 生成 | 禁止（重新生成会覆盖） |
| `*Mapper.java` | 生成 | 禁止 |
| `*CrudService.java` | 生成 | 禁止（自定义逻辑写在 `*Service.java`） |
| `*CrudController.java` | 生成 | 禁止（自定义端点写在 `*Controller.java` 或专用集成 Controller） |
| `*Service.java`（无 Crud 前缀） | 手写 | 可以 |
| `*Controller.java`（无 Crud 前缀） | 手写 | 可以 |

**典型场景：给模块加 deploy/publish/test 等集成端点**

- 错误做法：在 `AuthPolicyCrudController.java` 里加 `@PostMapping("/{id}/deploy")`
- 正确做法：创建 `PolicyIntegrationController.java`（无 class-level `@RequestMapping`，方法用完整路径），或在该域的 `*Controller.java` 中添加

**判断标准**：文件名含 `Crud` = 生成物，不碰。文件名不含 `Crud` = 手写，可以加逻辑。

## 【Extension】怎么扩展

- 新模块：先确认有 model.yaml → scaffold → 注册到 modules.yaml
- 复杂业务逻辑：在 Domain 层实现，不放 Controller
- 跨域调用：通过 Application 层编排，不在 Domain 层直接引用其他域
- 新增后端集成端点（deploy/publish/test 等）：创建独立的 `*IntegrationController.java` 或域级 `*Controller.java`，**禁止改 `*CrudController.java`**

## 【Cross-ref】改了这里还要看哪里

| 这里变了 | 同步检查 |
|:---|:---|
| 新增 Entity 字段 | → `_technical/tables.yaml` 是否登记 |
| 新增 Controller | → `_technical/modules.yaml` 是否注册 |
| API 接口变更 | → 前端 `api/index.ts` 是否同步 |
| model.yaml 变更 | → 重新 gen-schema → 前端 schema.ts 同步；重新 gen-backend → 后端四件套同步 |
| 重新 gen-backend | → 自定义逻辑不受影响（前提：没人误改 `*Crud*.java`） |
