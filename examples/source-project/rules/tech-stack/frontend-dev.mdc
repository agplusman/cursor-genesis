---
description: 前端开发规范——ListPageEngine 驱动、schema 生成、模态切换
globs: frontend/**
---

# 前端开发规范

> 从原 `project-conventions.mdc` 前端部分拆出，增加四要素结构
> 服务模态：**全部三种模态**（前端是模态投影的执行层）

## 【Scope】管了什么

```
frontend/src/
├── views/              # 页面（按业务模块，大部分由 ListPageEngine 动态渲染）
│   ├── asset/          # 资产（唯一有自定义 index.vue）
│   ├── identity/       # 身份管理（4 模块）
│   ├── policy/         # 策略控制（12 模块）
│   ├── affairs/        # 事务处理（10 模块）
│   ├── posture/        # 态势分析（3 模块）
│   ├── knowledge/      # 安全知识（6 模块）
│   ├── dashboard/      # 大盘（asset-posture）
│   └── placeholder.vue # 占位页
├── layouts/            # MainLayout（菜单 + 路由 + 三模态切换）
├── components/         # 通用组件
│   ├── ListPageEngine.vue   # 列表引擎（含模态字段过滤）
│   ├── ModelEditor.vue      # 开发模态：model.yaml 可视化编辑器
│   └── AcceptanceBubble.vue # 验收模态：合同项映射悬浮球
├── stores/             # Pinia 状态管理
│   └── modeStore.ts    # 产品模态 Store（dev/acceptance/user）
├── config/             # 配置
│   ├── moduleRegistry.ts     # 模块注册（双轨分组）
│   ├── menu.ts               # 菜单生成（按模态自动切换）
│   └── acceptanceMapping.ts  # 验收合同项映射数据
├── router/             # moduleRegistry 动态路由
└── utils/              # 工具函数
frontend/
└── vite-dev-model-api.ts  # Vite 插件：model.yaml 读写 API（仅 dev）
```

**核心机制**：每个模块提供 `schema.ts` + `api/index.ts`，ListPageEngine 按 objectCode 动态加载，无需手写 `.vue` 页面。

## 【Workflow】怎么用

### 新模块前端上线流程

1. 确认 `docs/domain/<子域>/<模块>/model.yaml` 已完成
2. 运行 `cd tools && node gen-schema.js <模块名>` 生成 schema.ts + api
3. 在 `moduleRegistry.ts` 注册模块（objectCode、路由、菜单分组）
4. ListPageEngine 自动渲染列表页 —— **不需要手写 Vue 文件**

### 需要自定义页面的情况

- 复杂交互（如资产的条件列宽、类型专用字段）
- Dashboard 类页面（图表组合）
- 多步表单/向导流程

自定义时仍复用 `useTable` + `useTableColumnWidth` + `el-table` 组件模式。

### 技术选型

- Vue 3 + Composition API + TypeScript
- Element Plus 组件库
- Pinia 状态管理
- ECharts 图表
- 列宽动态优化：见 `docs/frontend/list-column-width-optimization.md`

## 【Extension】怎么扩展

- 新模块：99% 情况走 gen-schema 自动生成，不手写
- 自定义列表：创建 `views/<模块>/index.vue`，覆盖 ListPageEngine 默认渲染
- 新组件：放 `components/`，PascalCase 命名

## 【Cross-ref】改了这里还要看哪里

| 这里变了               | 同步检查                                                     |
| :--------------------- | :----------------------------------------------------------- |
| schema.ts              | 来源是 model.yaml，**禁止直接手改** schema.ts                |
| api/index.ts           | → 后端 Controller 路径是否匹配                               |
| moduleRegistry.ts      | → menu.ts 自动重建菜单；检查 devGroup + acceptanceGroup      |
| MainLayout.vue         | → 路由和菜单分组是否一致                                     |
| model.yaml 的 `modes`  | → 重新 gen-schema → schema.ts 中 modes 字段更新              |
| acceptanceMapping.ts   | → AcceptanceBubble 的合同项显示是否匹配                      |
| modeStore.ts           | → 全局模态状态，影响菜单、字段过滤、组件可见性               |

## 模态实现机制

### 数据流（两条管线）

```
管线 1：构建时推导
model.yaml
  ├── fields[].applicableTo   → 字段适用哪种资产类型
  ├── fields[].modes          → 字段面向哪种受众
  └── viewModes               → 每个视图的 defaultFilters / toolbar / rowActions / modes
       ↓
gen-schema.js
  ├── applicableTo 不匹配 → 自动计算 hiddenColumns
  ├── defaultFilters 锁定的字段 → 自动计算 hiddenColumns
  └── modes / toolbar / rowActions → 透传
       ↓
schema.ts (viewModeOverrides + 各字段携带 applicableTo / modes)

管线 2：运行时解析
ListPageEngine
  ├── isFieldHiddenByMode(field, mode, viewMode) → 三维可见性
  ├── dev 模态：不应用 hiddenColumns / toolbar / rowActions 裁剪
  ├── user 模态：applicableTo 过滤 + modes 过滤 + viewModes.modes.user 覆盖
  └── acceptance 模态：acceptanceMapping.ts 锚定菜单/合同项（字段走 modes.acceptance）
```

### 字段可见性：两种机制，各管一个维度

| 机制 | 回答的问题 | 配置位置 | 示例 |
|:---|:---|:---|:---|
| **applicableTo** | 这个字段跟哪种资产类型有关？ | field 上 | `applicableTo: [ip, service, data]` |
| **modes** | 这个字段给谁看？ | field 上（兜底）或 viewModes.{view}.modes（视图级覆盖） | `modes: { user: { hidden: true } }` |

- **不写 applicableTo = 全类型可见**
- **不写 modes = 全模态可见**
- **dev 模态跳过两者，全量透出**

`isFieldHiddenByMode` 实现（ListPageEngine.vue）：

```typescript
function isFieldHiddenByMode(field, mode, viewMode?) {
  if (mode === 'dev') return false
  if (field.applicableTo && viewMode && TYPE_VIEW_MODES.has(viewMode)) {
    if (!field.applicableTo.includes(viewMode)) return true
  }
  return field.modes?.[mode]?.hidden === true
}
```

### 各模态的具体行为

| 维度 | 开发模态 | 验收模态 | 用户模态 |
|:---|:---|:---|:---|
| 菜单 | devGroup/devSubGroup 分组 | acceptanceGroup/acceptanceSubGroup 分组 | 同开发（过滤 `userHidden`） |
| 字段 | **全量渲染**（忽略 applicableTo 和 modes） | modes.acceptance.hidden 过滤 | applicableTo 过滤 + modes.user.hidden 过滤 |
| hiddenColumns | **不应用**（开发者看全量） | 应用（gen-schema 自动推导） | 应用（gen-schema 推导 + viewModes.modes.user.hiddenColumns） |
| toolbar / rowActions | **应用 viewMode 级配置**（同用户模态） | 应用 viewMode 级配置 | 应用 viewMode 级配置 |
| defaultFilters | 应用（数据范围不是 UI 约束） | 应用 | 应用 |
| Model Editor | 可用（工具栏"模型编辑"按钮） | 隐藏 | 隐藏 |
| AcceptanceBubble | 隐藏 | 显示（右下角悬浮球→合同项映射） | 隐藏 |
| `acceptanceOnly` 模块 | 不出现在菜单 | 出现 | 不出现 |

> **Dev = 全量版用户模态**（2026-03-14）：viewMode 的 toolbar/rowActions 全模态生效，dev 只多出全量字段+全量列+Model Editor+技术模块。推导：`knowledge-graph/meta/derivation/dev-mode-as-full-user-mode-2026-03-14.md`

### 模态切换

- UI 入口：MainLayout 顶栏右侧 `el-radio-group`（开发 / 验收 / 用户）
- 持久化：`localStorage` key `smc-product-mode`
- Store：`stores/modeStore.ts`（Pinia），提供 `currentMode`、`isDevMode`、`isAcceptanceMode`、`isUserMode`

### model.yaml 配置约定

```yaml
fields:
  - code: ipAddress
    applicableTo: [ip, service, data]   # 领域语义：适用哪些资产类型
    # 不写 applicableTo = 全类型可见

  - code: sourceTaskId
    modes:                              # 受众语义：给谁看
      user: { hidden: true }
      acceptance: { hidden: true }
    # 不写 modes = 全模态可见

viewModes:
  ip:
    defaultFilters: { assetTypeCode: IP, lifecycleStatus: FORMAL }
    toolbar: [export]
    rowActions: [view, edit, archive, registerDeviceIdentity]
    modes:                              # 视图级覆盖（优先于字段级 modes）
      user:
        hiddenColumns: [assetCategoryId, isIgnored, approvalStatus]
```

### 调试字段不见了？

按顺序检查：
1. 当前是什么模态？dev 模态应全量可见
2. 字段的 `applicableTo` 是否包含当前 viewMode？
3. 字段的 `modes.{当前模态}.hidden` 是否为 true？
4. viewModes.{view}.modes.{模态}.hiddenColumns 是否包含该字段？
5. gen-schema 是否重新运行过？（`cd tools && node gen-schema.js asset`）

### 调试组件行为不符预期？

用户说"下拉是平铺的""不是树形""组件不对""不好看"时，按顺序排查：

1. **本体**：读该子域 `ontology-extracted.md`，确认概念的**预期结构**（层级树？枚举列表？多对多？）
2. **model.yaml**：检查字段的 `component` 和 `fallbackOptions` 是否忠于本体结构
   - 树形概念 → component 应为 `tree-select`，fallbackOptions 应使用 `children` 嵌套
   - 枚举概念 → component 应为 `select`，fallbackOptions 平铺即可
3. **gen-schema.js**：确认转换逻辑对该 component 类型的处理（是否原样透传 fallbackOptions）
4. **ListPageEngine.vue**：grep 该 component 类型的渲染分支，确认是否使用了正确的 Element Plus 组件（如 `el-tree-select` vs `el-select`）

**核心原则**：本体定义"应该长什么样"，model.yaml 配置"怎么渲染"，ListPageEngine 执行"实际渲染"。三层任一不一致 = bug 来源。**禁止**跳过本体直接读代码猜组件该怎么渲染。
