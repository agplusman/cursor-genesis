---
description: 本体驱动开发工作流——提取、编写、校验、维护、应用全流程
globs: docs/domain/**,docs/research/**,docs/architecture/**
---

# 本体驱动开发工作流

> 合并自原 `ontology-maintenance.mdc`，增加四要素结构和模态标注
> 服务模态：**开发模态**（主），验收模态参考本体确认标书覆盖范围

## 【Scope】管了什么

本目录 (`docs/domain/`) 是业务的**语义地图**，采用业务本体 + 技术本体双轨制：

| 类型 | 位置 | 回答的问题 |
|:---|:---|:---|
| 业务本体 | 各子域 `ontology-{extracted,satellite,extended}.md` | 有什么概念、谁和谁有什么关系 |
| 技术本体 | `_technical/tables.yaml`, `modules.yaml`, `mapping.yaml` | 表怎么拆、模块怎么依赖、概念落在哪张表 |
| model.yaml | 各子域目录 | 从本体到前后端代码生成的配置 |
| 方法论 | README.md + 5 篇方法论文档 | 怎么提取、怎么写、怎么校验、怎么维护 |

业务本体三级分类：核心（`ontology-extracted.md`）→ 卫星（`ontology-satellite.md`）→ 扩展（`ontology-extended.md`）

## 【Workflow】怎么用

### 场景 1：为新子域提取本体

1. **必读**：`ontology-extraction-rules.md`（五步流程） + `ontology-document-guide.md`（文档规范）
2. **分类判断**：按 document-guide §〇 判断核心/卫星/扩展，选择对应文件命名
3. **执行**：规格说明 → 素材扫描（显式摘录，不依赖记忆）→ 实现层对照 → 概念化 → 形式化
4. **校验**：读 `ontology-validation-rules.md`，执行正向→反向→交叉三步校验
5. **技术本体同步**：新表 → `_technical/tables.yaml`；新模块 → `_technical/modules.yaml`；映射 → `_technical/mapping.yaml`

### 场景 2：从本体生成配置

1. 读 `ontology-to-config-workflow.md`
2. 本体概念 → `model.yaml` 字段映射
3. `cd tools && node gen-schema.js <模块名>` → 生成 `schema.ts`
4. ListPageEngine 自动加载，无需手写 Vue 页面

### 场景 3：变更已有概念/字段

1. 读该子域本体 + `_technical/mapping.yaml`
2. 按 `ontology-maintenance-guide.md` §三 更新流程执行
3. **双轨同步**：业务概念变化 → 更新 ontology；表/模块变化 → 更新 `_technical/`
4. 执行 `ontology-validation-rules.md` 反向校验

### 场景 4：设计讨论结论落位

1. 评估哪些变化属于业务概念、哪些属于表/模块
2. 分别更新对应本体
3. 映射变化时同步 `mapping.yaml`

### 场景 5：调试概念/数据缺失

当用户报告"下拉框没数据""缺字段""数据不对"时，**先做本体 vs 实现的 diff**：

1. 读该子域 `ontology-extracted.md`，确认概念在本体中是否存在（类 + 对象属性）
2. 读对应 `model.yaml`，检查该概念是否有对应字段（type: ref / enum / 缺失）
3. 读 `_technical/modules.yaml`，确认被引用模块是否已实现
4. 若本体有、实现无 → 实现缺口；若实现有、数据无 → 种子数据缺失；若配置不一致 → 同步问题

**禁止**：跳过本体直接 grep 代码或搜索 mock-server。本体是概念权威，先确认"应该有什么"再查"实际有什么"。

## 【Extension】怎么扩展

- 新子域：先判断归属（见 `domain-architecture.md` §三），再选本体级别（核心/卫星/扩展）
- 新概念归入已有子域：追加到该子域 ontology，同步 `_technical/`
- model.yaml 扩展字段：参考 `metadata-schema-design.md` 的 EAV 模式

## 【Cross-ref】改了这里还要看哪里

| 这里变了 | 同步检查 |
|:---|:---|
| ontology-extracted.md | → `_technical/mapping.yaml` 是否需要更新 |
| `_technical/tables.yaml` | → 对应子域 ontology 是否有该概念 |
| model.yaml | → `schema.ts`（重新 gen-schema）→ 前端是否生效 |
| domain-architecture.md | → `domain-progress.md` 是否需要更新 |

## 禁止

- 不得只更新业务本体而忽略技术本体（或反之）
- 不得跳过 extraction-rules 直接编写新子域本体
- 不得跳过分类判断直接命名文件
