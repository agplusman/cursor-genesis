---
description: 安管工作流路由——根据用户意图和产品模态派发到对应流程和文档
alwaysApply: true
---

# 工作流路由器

> 依赖：`product-mode.mdc`（先识别模态，再识别意图）

## 设计即权威（全局约束）

> 完整定义见 `design-is-authority.mdc`，以下为路由层摘要。

**无条件生效**——不论用户是否提到设计文档，Agent 的认知路径都必须从设计开始：

```
设计文档 → 本体 → model.yaml → 确定结构/命名/逻辑 → 代码（仅兼容性确认）
```

- 旧代码**不是**结构、命名、逻辑的参考来源——它只用于兼容性评估
- 读代码前自问：这个信息设计文档里有没有？有就不读代码
- 下方路由表每条的"Agent 首读文档"就是该任务的设计入口，**禁止跳过直接读代码**

## 路由前置：任务分类

当用户指向设计文档的 TODO/遗留项/增强项，或意图信号不直接匹配路由表时，**先分类再动手**：

1. **定位模块**：从文件名、内容关键词判断涉及哪个子域（asset? asset-type? identity?）
2. **定位层级**：前端引擎？model.yaml 配置？后端 API？数据库 schema？
3. **定位类型**：新增功能？增强现有？Bug 修复？配置调整？
4. **读设计链**：该模块 ontology → model.yaml → 已有设计文档
5. **评估变更路径**：基于设计文档判断需要改什么，**仅在确认需要改代码时才读代码**

分类完成后，用最匹配的路由表条目执行。

## 路由表

识别用户意图后，按当前模态派发到对应工作流。每条路由定义 Agent **首先读什么**。

### 一、开发模态路由

| 意图信号 | 工作流 | Agent 首读文档 |
|:---|:---|:---|
| "实现 X 模块""脚手架""scaffold" | 模块实现 | 该模块 `model.yaml` → `tools/gen-schema.js` → 后端 scaffold 规范 |
| "新增功能""新概念""新子域" | 本体驱动设计 | `docs/domain/domain-architecture.md` → `ontology-extraction-rules.md` → `ontology-document-guide.md` |
| "改字段""加字段""改表" | 变更同步 | `docs/domain/ontology-maintenance-guide.md` → 相关子域 ontology → `_technical/mapping.yaml` |
| "前端""页面""列表""schema" | 前端开发 | `docs/frontend/list-page-template-spec.md` → 该模块 `schema.ts` |
| "后端""接口""API""Service" | 后端开发 | `_technical/modules.yaml` → 该模块包路径 → `AbstractCrudService` |
| "配置""元数据""EAV" | 元数据管理 | `docs/domain/asset/metadata-schema-design.md` → `asset-meta-field/model.yaml` |
| "集成""对接""外部系统" | 集成开发 | `docs/integration/interface-catalog.md` → 对应系统文档 |
| "调研""选型""技术方案" | 技术调研 | `docs/research/` 已有结论 → 补充调研 |
| "态势""大盘""dashboard" | 态势开发 | `docs/domain/posture/ontology-extracted.md` → `AssetPostureService.java` |
| "规则引擎""条件匹配" | 规则中心 | `docs/research/lightweight-rule-engine/COMPARISON-AND-RECOMMENDATION.md` → `RuleService` |
| "定时任务""调度" | 任务中心 | `docs/research/java-task-scheduler/COMPARISON-AND-RECOMMENDATION.md` → `TaskManagementService` |
| "没数据""数据不对""下拉框空""缺字段""表单不对" | 概念完整性排查 | 该子域 `ontology-extracted.md`（确认应有什么）→ `model.yaml`（确认配了什么）→ `_technical/modules.yaml`（确认后端有没有）→ 精准定位缺口 |
| "平铺""不是树形""下拉不对""组件不对""渲染不对""不好看不好用" | 组件行为排查 | 该子域 `ontology-extracted.md`（确认概念应有的结构：树形？枚举？）→ `model.yaml`（字段 component + fallbackOptions 是否忠于本体）→ `ListPageEngine.vue`（渲染分支是否正确处理该组件类型） |
| "页面报错""接口 404""500 错误" | 接口排查 | `_technical/modules.yaml`（确认 API 路径）→ 后端 Controller → 前端 api/index.ts |
| 指向设计文档 TODO/增强/遗留项，或"增强""做一下""实现这个" | 设计增强实施 | 设计文档（定位模块+层级）→ 该模块 ontology → model.yaml → 评估变更路径（model config? gen-schema? 前端引擎? 后端 API?）→ 仅确认需改代码时才读代码 |
| "写测试""单测""集成测试""test""vitest""junit""测试覆盖" | 测试编写 | 被测模块的 `ontology-extracted.md`（理解概念边界和业务规则）→ `model.yaml` / `_technical/modules.yaml`（确认模块结构和接口）→ 已有设计文档（架构决策、数据流）→ 被测代码的**接口签名**（不读实现细节）→ 编写测试。**禁止**：全量读取被测代码后再写测试；应从设计文档理解"该测什么"，从接口签名理解"怎么调用" |
| "X 是什么""找找设计""从哪来的""未实现""有没有这个""状态是什么" | 概念溯源 | 该子域 `ontology-extracted.md`（本体有没有这个概念）→ `_technical/tables.yaml` + `modules.yaml`（技术本体有没有）→ `docs/` 搜索相关设计文档 → 得出结论。**禁止**起手 grep 代码仓库 |

### 二、验收模态路由

| 意图信号 | 工作流 | Agent 首读文档 |
|:---|:---|:---|
| "验收菜单""有哪些页面" | 菜单对标 | `docs/acceptance/acceptance-menu-and-demo-plan.md` |
| "演示""走一遍流程" | 演示脚本 | `acceptance-menu-and-demo-plan.md` 的演示流程大纲 |
| "标书要求""规格说明" | 标书对照 | `docs/architecture/安全大脑需求规格说明书-身份管理.md` |
| "差什么""缺什么页面" | 差异分析 | `docs/acceptance/new-pages-classification-checklist.md` |

### 三、用户模态路由

| 意图信号 | 工作流 | Agent 首读文档 |
|:---|:---|:---|
| "用户怎么用""简化页面" | 用户界面调整 | `docs/domain/product-mode-architecture.md` §三 可配置性边界 |
| "加个字段让用户看到" | 元数据配置 | `asset-meta-field/model.yaml` → 字段展示控制 |
| "产品经理反馈" | PM 裁剪记录 | 记录到 `docs/domain/product-mode-architecture.md` 并更新 profile |

### 四、元活动路由（跨模态）

| 意图信号 | 工作流 | Agent 首读文档 |
|:---|:---|:---|
| "优化规则""改规则""规则不好用""Agent 行为不对""调试路径不对" | 规则优化 | `docs/learnings/rule-optimizations/` 已有记录 → 按空间内记录规范执行 |

## 路由失败时

如果用户意图无法匹配上述任何路由：

1. 执行"路由前置：任务分类"的 5 步分类
2. 若分类后仍不明确，问用户当前是在做什么（开发 / 验收准备 / 讨论用户体验）
3. 确认后按对应模态路由
4. 若完全是新场景，参考 `docs/domain/domain-architecture.md` §三 归属判断规则
