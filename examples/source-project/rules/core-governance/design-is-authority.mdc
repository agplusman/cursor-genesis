---
description: 设计即权威——代码结构/命名/逻辑从设计推导，旧代码仅用于兼容性评估
alwaysApply: true
---

# 设计即权威

> 全局约束，适用于所有代码任务（新增 / 修改 / 重构 / 排查），无例外。

## 一、核心原则

**设计文档是结构、命名、逻辑的唯一权威来源。旧代码不是。**

| 维度 | 权威来源 | 禁止来源 |
|:---|:---|:---|
| 结构（类/包/模块划分） | ontology + domain-architecture | 旧代码的包结构 |
| 命名（类名/方法名/字段名） | ontology 概念 + model.yaml 字段定义 | 旧代码的命名惯例 |
| 逻辑（业务流程/校验/联动） | ontology 业务规则 + 设计文档 | 旧代码的实现逻辑 |
| 字段与存储 | model.yaml + tables.yaml | 旧代码的 Entity/DTO 定义 |

当设计文档与旧代码冲突时，**以设计文档为准，改代码**。

## 二、旧代码的唯一合法用途 —— 兼容性

已存在的代码只在以下场景允许读取，且目的仅限评估兼容性：

| 场景 | 允许读什么 | 目的 |
|:---|:---|:---|
| 改动影响评估 | 调用方的接口签名（非实现） | 确认不破坏现有调用链 |
| Bug 定位 | 报错堆栈涉及的具体方法 | 精准定位 bug 根因 |
| 迁移评估 | 待迁移文件的公开接口 | 评估工作量和断点 |
| 前后端对接 | Controller / api 层签名 | 确认接口契约不断裂 |

**读代码前自问：这个信息设计文档里有没有？** 有→读设计文档。没有→说明设计文档有缺口，应先补设计再动手。

## 三、禁止行为（节省 60%+ token 的关键）

| 编号 | 禁止行为 | 替代做法 |
|:---|:---|:---|
| F1 | "先读代码再动手" | 第一步永远是读设计文档 |
| F2 | "找类似模块模仿着写" | 从设计推导结构，不从旧代码复制模式 |
| F3 | "读完所有相关代码再开始" | 设计文档已定义该写什么，定向读≤1个文件做兼容确认 |
| F4 | "参考旧代码的命名风格" | 命名从本体概念推导（ontology 中文→英文映射） |
| F5 | "全量扫描代码目录了解结构" | modules.yaml 有全部模块注册，model.yaml 有全部字段 |
| F6 | "读旧实现来理解架构" | 架构在 domain-architecture.md + ontology，不在代码里 |

## 四、正确认知路径

### 新增代码

```
ontology → model.yaml → 从设计推导结构和命名 → 写代码 → 兼容性确认
```

### 修改代码

```
设计文档确认目标状态 → model.yaml 确认字段 → 定向改目标方法（≤1个文件）
```

### 排查问题

```
ontology（应有什么）→ model.yaml（配了什么）→ modules.yaml（注册了什么）→ 精准定位缺口
```

### 重构 / 迁移

```
设计文档确认目标架构 → 契约映射表定位目标 → 基类理解模式 → 定向改文件
```

## 五、设计文档优先级

遇到信息冲突或不确定时，按以下优先级取信：

1. `ontology-extracted.md` — 概念权威（有什么、叫什么、关系是什么）
2. `model.yaml` — 配置权威（字段、组件、展示、校验）
3. `_technical/modules.yaml` — 技术注册权威（包路径、API 路径、状态）
4. `_technical/tables.yaml` — 存储权威（表、列、约束、索引）
5. `domain-architecture.md` — 架构权威（域划分、依赖方向、归属规则）
6. 代码 — **最低优先级**，仅兼容性参考
