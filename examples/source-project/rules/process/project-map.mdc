---
description: 安管项目全景图——模块清单、实现状态、关键文件索引
alwaysApply: true
---

# 项目全景图

> 数据来源：`docs/domain/_technical/modules.yaml`

## 一、六域架构

```
安全基础管理系统 ─┬─ asset（基础域）    14 个子模块，后端已实现
                 ├─ identity（基础域）  4 个子模块，后端已实现
                 └─ knowledge（支撑域） 6 个子模块，后端已实现
安全态势分析系统 ─── posture（流程域）   3 个子模块，后端已实现
安全策略控制系统 ─── policy（流程域）    12 个子模块，后端已实现
安全事务处理系统 ─── affairs（流程域）   10 个子模块，后端已实现
```

依赖方向：基础域 ← 流程域 ← 支撑域（基础域不依赖其他域）

## 二、实现状态总览

### 后端已实现（49 模块）

| 域 | 模块 | 包 | 备注 |
|:---|:---|:---|:---|
| asset | asset, asset-type, asset-category, asset-domain, asset-meta-field, discover-task, discover-result, asset-ignore | com.secmgr.asset | 核心 CRUD + 发现任务(三道管线+uniqueKey去重) |
| identity | personnel, organization, device, application | com.secmgr.identity | 四类身份 |
| policy | baseline, protection, auth, authorization, security-plan, compliance-plan, role, task-type, disposal-task, analysis-task, analysis-indicator, linkage-action | com.secmgr.policy | 12 个策略模块，全部 CRUD |
| affairs | approval, work-order, risk-warning, vuln-check, asset-vulnerability, baseline-check, baseline-noncompliance, incident, violation-notice, audit-log | com.secmgr.affairs | 10 个事务模块，全部 CRUD |
| posture | analysis-model, alert, indicator | com.secmgr.posture | 态势分析 + 告警 + 指标 |
| knowledge | vulnerability, vuln-type, blackwhitelist, directory, receive-task, publish-task | com.secmgr.knowledge | 知识库 + 收发任务 |
| system | sys-user, sys-audit-log, sys-dict | com.secmgr.system | 系统管理 |
| infra | task, rule, export | com.secmgr.task/rule/export | 通用能力 |

### 前端

**全部 49 个模块已有 schema.ts + api**，由 ListPageEngine 动态渲染，前后端全链路对接。

### 三模态架构（已实现）

| 组件 | 文件 | 职责 |
|:---|:---|:---|
| Mode Store | `frontend/src/stores/modeStore.ts` | 产品模态状态管理（Pinia + localStorage） |
| 菜单生成 | `frontend/src/config/menu.ts` | 从 moduleRegistry 按模态自动生成菜单树 |
| 模块注册 | `frontend/src/config/moduleRegistry.ts` | 双轨分组（devGroup + acceptanceGroup） |
| 字段级过滤 | `ListPageEngine.vue` | 按 `modes` 字段裁剪列/查询/表单 |
| Model Editor | `frontend/src/components/ModelEditor.vue` | 开发模态专属：可视化编辑 model.yaml |
| Dev Model API | `frontend/vite-dev-model-api.ts` | Vite 插件：model.yaml 读写 + gen-schema + HMR |
| 验收悬浮球 | `frontend/src/components/AcceptanceBubble.vue` | 验收模态专属：合同项映射 |
| 验收映射 | `frontend/src/config/acceptanceMapping.ts` | objectCode → 标书条款映射数据 |

## 三、关键文件索引

| 场景 | 首读文件 |
|:---|:---|
| 了解业务域划分 | `docs/domain/domain-architecture.md` |
| 查看某模块的业务本体 | `docs/domain/<子域>/ontology-extracted.md` |
| 查看某模块的配置 | `docs/domain/<子域>/model.yaml` 或子目录 `model.yaml` |
| 查看表结构 | `docs/domain/_technical/tables.yaml` |
| 查看模块注册 | `docs/domain/_technical/modules.yaml` |
| 查看概念↔表映射 | `docs/domain/_technical/mapping.yaml` |
| 本体方法论 | `docs/domain/README.md`（核心入口） |
| 验收菜单 | `docs/acceptance/acceptance-menu-and-demo-plan.md` |
| 三模态设计与实现 | `docs/domain/product-mode-architecture.md` |
| 元引擎架构 | `docs/domain/meta-engine-architecture.md` |
| 集成接口 | `docs/integration/interface-catalog.md` |
| 调研结论 | `docs/research/<主题>/COMPARISON-AND-RECOMMENDATION.md` |
| 前端规范 | `docs/frontend/list-page-template-spec.md` |
| 产品模态切换逻辑 | `frontend/src/stores/modeStore.ts` + `frontend/src/config/menu.ts` |
| 验收合同项映射 | `frontend/src/config/acceptanceMapping.ts` |

## 四、技术栈

- **后端**：Java 17 + Spring Boot + MyBatis-Plus + JobRunr + SpEL 规则
- **前端**：Vue 3 + TypeScript + Element Plus + ECharts + ListPageEngine + 三模态切换
- **Mock**：Express + Faker.js（`mock-server/`）
- **工具**：`tools/gen-schema.js`（model.yaml → schema.ts，支持 `modes` 字段透传）
