---
description: 身份管理模块开发规范
globs: backend/**/identity/**,frontend/**/identity/**,docs/domain/identity/**
---

# 身份管理模块规范

## 领域模型

- **核心实体**：Identity（身份）→ PersonIdentity（人员）、Organization（组织机构）、DeviceIdentity（设备）、AppIdentity（应用）
- **本体**：`docs/domain/identity/ontology-extracted.md`
- **依赖方向**：被 policy/affairs 引用；引用 asset（OnboardedAsset 可注册为 DeviceIdentity）

## 四类身份

| 身份类型 | 表 | API 前缀 | 关键特征 |
|:---|:---|:---|:---|
| 人员身份 | personnel_info | /api/v1/identity/personnel | GA/T380 12 位编码、物理凭证、可信设备、生物特征 |
| 组织机构 | organization_info | /api/v1/identity/organization | 树形结构、机构代码 |
| 设备身份 | device_info | /api/v1/identity/device | 从已上架终端资产注册、核验/冻结 |
| 应用身份 | application_info | /api/v1/identity/application | 应用系统注册 |

## 后端结构

- **包路径**：`com.secmgr.identity`
- **服务**：PersonnelService、OrganizationService、DeviceService、ApplicationService
- **全部已实现**

## 跨域引用

- `Asset.hasOwner` → PersonIdentity
- `Asset.belongsToDept` → Organization
- OnboardedAsset（终端）→ 可注册为 DeviceIdentity
- OnboardedAsset（应用）→ 可注册为 AppIdentity

## 模态差异

| 维度 | 开发模态 | 验收模态 | 用户模态 |
|:---|:---|:---|:---|
| 展示字段 | 全量 | 标书身份管理章节描述 | 核心字段（姓名/编码/状态） |
| 操作 | CRUD + 凭证管理 + 目录发布 | 演示 F7 注册→发布流程 | 查看 + 基本编辑 |
